/*CREATE TABLE IF NOT EXISTS users (
    id INTEGER PRIMARY KEY,
    username TEXT,
    password TEXT,
    user_type TEXT)*/

SELECT * FROM users

/*INSERT INTO users (username, password, user_type)
VALUES('poorna','123456','farmer')*/
